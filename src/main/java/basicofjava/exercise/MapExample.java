package basicofjava.exercise;


/**
 * Map
 * - Key, Value 형식으로 저장되는 자료구조
 *   ㄴ K - the type of keys maintained by this map
 *   ㄴ V - the type of mapped values
 *
 * [Implmenting classes]
 * - AbstractMap, Attributes, AuthProvider, ConcurrentHashMap, ConcurrentSkipListMap,
 * EnumMap, HashMap, Hashtable, IdentityHashMap, LinkedHashMap,
 * PrinterStateReasons, Properties, Provider, RenderingHints,
 * SimpleBindings, TabularDataSupport, TreeMap, UIDefaults, WeakHashMap
 */
public class MapExample {
    /* exec */
    public void exec() throws Exception {

    }
}

package programmers;

import programmers.kakao.FailureRate;
import programmers.naverfinancial.Sample;

public class Programmers {
    public static void main(String[] args) {
        // N개의 최소 공배수
        Sample sample = new Sample();
        sample.exec();
    }

    private static void level102(){
        // 수박수
        //Subaksu subaksu = new Subaksu();
        //subaksu.exec();

        // 입력된 숫자 까지에 존재하는 모든 소수의 갯수를 구하여라.
        //FindPrimeNumb findPrimeNumb = new FindPrimeNumb();
        //findPrimeNumb.exec();

        // 주어진 배열의 원소를 조합하여 생성 가능한 소수를 찾으시오.
        //MakePrimeNumb makePrimeNumb = new MakePrimeNumb();
        //makePrimeNumb.exec();

        // 문자열 배열속에서 Kim 이 존재하는 인덱스 찾기 - 1점짜리
        //FindMrKim findMrKim = new FindMrKim();
        //findMrKim.exec();

        // 문자열 길이 4와 6으로 이루어진 숫자를 판독 하시오.
        //ControlStrings controlStrings = new ControlStrings();
        //controlStrings.exec();

        // 스트링 코드를 역순으로 정렬하시오.
        //StringSort stringSort = new StringSort();
        //stringSort.exec();

        // 대소문자가 섞인 문자열에서 p 와 y의 개수를 비교하여 같으면 true 를 다르면 false 를 리턴하다. - 1점짜리
        //CountPandY countPandY = new CountPandY();
        //countPandY.exec();

        // 주어진 문자열 배열들의 원소 위치를 지정하면 해당 글자를 기준으로 정렬을 실행하라.
        //StringSort2 stringSort2 = new StringSort2();
        //stringSort2.exec();

        // 두 정수가 주어질때 두 정수 사이의 모든 정수의 합을 계산하씨오.
        //SumOfTwoInteger  sumOfTwoInteger = new SumOfTwoInteger();
        //sumOfTwoInteger.exec();

        // 입력된 숫자 배열과 Divisor 를 이용하여 나누어 떨어지는 숫자들의 배열을 뱉으세요.
        //DivideArray divideArray = new DivideArray();
        //divideArray.exec();

        // 배열내 연속된 숫자는 하나만 남기고 모두 삭제한다.
        //DistinctSameNumber distinctSameNumber = new DistinctSameNumber();
        //distinctSameNumber.exec();

        // 다트게임 (입력된 문자열의 특징을 분석하여 스코어를 계산하라)
        //DartGame dartGame = new DartGame();
        //dartGame.exec();

        // 부족한 금액 계산하기
        //InsufficientAmount insufficientAmount = new InsufficientAmount();
        //insufficientAmount.exec();

        // 최소직사각형 - 가로 세로 길이가 제각각일때 모두 수렴 가능한 최소직사각형을 구하라
        //MinimumRectangle minimumRectangle = new MinimumRectangle();
        //minimumRectangle.exec();

        // 비밀지도
        // SecretMap secretMap = new SecretMap();
        // secretMap.exec();

        // 최소공약수 최대공배수를 구하라
        //CommonMultiple commonMultiple = new CommonMultiple();
        //commonMultiple.exec();

        // 크레인 문제
        //Crane crane = new Crane();
        //crane.exec();

        // 잡다한 파일
        //Exercise exercise = new Exercise();
        //exercise.exec();
    }

    private static void level101(){
        // 끝내지못한 경주
        //UncompletedRace uncompletedRace = new UncompletedRace();
        //uncompletedRace.exec();

        // 전화번호 접두어
        //ListOfPhoneNumbers listOfPhoneNumbers = new ListOfPhoneNumbers();
        //listOfPhoneNumbers.exec();

        // 위장술
        //Camouflage camouflage = new Camouflage();
        //camouflage.exec();

        // 수포자
        //NoMoreMath noMoreMath = new NoMoreMath();
        //noMoreMath.exec();

        // 연속된 숫자를 한번만
        //DistinctNumb distinctNumb = new DistinctNumb();
        //distinctNumb.exec();

        // 3개의 숫자 합이 소수인 경우의 수는?
        //CountingPrimeNumber countingPrimeNumber = new CountingPrimeNumber();
        //countingPrimeNumber.exec();

        // 개발완공율과 수행속도
        //Development development = new Development();
        //development.exeC();

        // 몇번째로 출력되는가
        //Printer printer = new Printer();
        //printer.exec();

        // 배열 Split 정렬후 K번째 숫자는?
        //KNumber kNumber = new KNumber();
        //kNumber.exec();

        // 입력된 숫자로 가장 큰 숫자조합을 찾아내세요
        //BiggestNumber biggestNumber = new BiggestNumber();
        //biggestNumber.exec();

        // 임의의 빈칸과 로또 최고 최저 등수
        //LottaryScore lottaryScore = new LottaryScore();
        //lottaryScore.exec();

        // 허용가능한 ID 로 치환
        //RecommenNewID recommenNewID = new RecommenNewID();
        //recommenNewID.exec();;

        // 영문표기를 읽어 숫자로 변환하기
        //NumbersAndEnglishWords numbersAndEnglishWords = new NumbersAndEnglishWords();
        //numbersAndEnglishWords.exec();

        // 최단거리 키패드 누르기
        //TouchKeyPad touchKeyPad = new TouchKeyPad();
        //touchKeyPad.exec();

        // 숫자와 부호로 이루어진 배열로 전체 합을 구하라
        //PlusMinus plusMinus = new PlusMinus();
        //plusMinus.exec();

        // 숫자배열중 존재하지 않는 수들의 합을 구하라. - 1점짜리.
        // SumOfNoVisible sumOfNoVisible = new SumOfNoVisible();
        // sumOfNoVisible.exec();

        // 내적을 구하라 - 1점짜리.
        //InnerProduct innerProduct = new InnerProduct();
        //innerProduct.exec();

        // 모의고사
        //MockExam mockExam = new MockExam();
        //mockExam.exec();

        // 약수의 개수를 기준으로 구간 합을 구하여라
        //CountDivisorAndSummary countDivisorAndSummary = new CountDivisorAndSummary();
        //countDivisorAndSummary.exec();

        // 입력된 정수는 3진법상에서 앞뒤를 뒤집은 후, 이를 다시 10진법으로 표현하시오.
        //TripleConversion tripleConversion = new TripleConversion();
        //tripleConversion.exec();

        // 입력받은 월, 일에 해당하는 요일을 리턴하시오.
        //Year2016 year2016 = new Year2016();
        //year2016.exec();

        // 입력받은 숫자배열 중 두 수를 이용하여 만들 수 있는 모든 수를 생성하시오.
        //MakeNumbersForSum makeNumbersForSum = new MakeNumbersForSum();
        //makeNumbersForSum.exec();

        // 할당된 예산으로 배분가능한 최대 부서수를 구하여라
        //Budget budget = new Budget();
        //budget.exec();

        // 여벌의 체육복으로 커버 가능한 최대 인원 수를 구하여라
        //TrainingSuite trainingSuite = new TrainingSuite();
        //trainingSuite.exec();

        // 게임의 참여자가 계류중인 스테이지 별 실패율을 계산하고 그 스테이지를 내림차순으로 정렬하라.
        FailureRate failureRate = new FailureRate();
        failureRate.exec();

        // 주어진 포키몬 카드로 구성가능한 최대 카드 수 산출
        //Pokemon pokemon = new Pokemon();
        //pokemon.exec();

        // 영문 스트링의 글자순서에 따라 대/소문자로 변환하시오
        //StrangeStrings strangeStrings = new StrangeStrings();
        //strangeStrings.exec();

        // 나머지가 1이 될수 있는 가장 작은 수를 구하시오. - 1점짜리
        //FindNumber1 findNumber1 = new FindNumber1();
        //findNumber1.exec();

        // 입력문자를 지정한 오프셋만큼 아스키코드를 이동하였을때 값은?!?
        //CaesarPassword caesarPassword = new CaesarPassword();
        //caesarPassword.exec();

        // 입력된 문자열의 숫자를 실제 숫자로 변환해주세요.
        //StringToNumber stringToNumber = new StringToNumber();
        //stringToNumber.exec();
    }
}
